default:
  primary: yahoo
  fallback:
    - alpaca

# First-release coverage: NYSE + NASDAQ only (tested REST + streaming).
# AMEX / CBOE / CME are intentionally omitted until end-to-end support exists.
exchanges:
  NASDAQ:
    primary: yahoo
    fallback:
      - alpaca
  NYSE:
    primary: yahoo
    fallback:
      - alpaca
  CRYPTO:
    primary: crypto
    fallback:
      - yahoo
